"""Gym & nutrition tracker with a WhatsApp/CLI interface.

Core logic (this package) is pure-Python and has no third-party
dependencies, so it can run anywhere. The WhatsApp adapter lives in
``bot.py`` at the repo root.
"""

__version__ = "0.1.0"
